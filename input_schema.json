{
  "title": "LeadGap AI input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "country": {
      "title": "Country",
      "type": "string",
      "description": "Target country for local business discovery.",
      "enum": ["US", "UK", "CA", "AU", "NZ", "IE", "DE", "FR", "ES", "IT", "NL", "PL", "BR", "AE", "SG"],
      "enumTitles": ["United States", "United Kingdom", "Canada", "Australia", "New Zealand", "Ireland", "Germany", "France", "Spain", "Italy", "Netherlands", "Poland", "Brazil", "United Arab Emirates", "Singapore"],
      "default": "UK",
      "editor": "select"
    },
    "city": {
      "title": "City",
      "type": "string",
      "description": "City for automatic discovery (e.g. Manchester, Austin, Berlin).",
      "editor": "textfield",
      "prefill": "Manchester"
    },
    "businessType": {
      "title": "Business type / niche",
      "type": "string",
      "description": "Niche to discover, e.g. Dentist, Plumber, Beauty Salon, Lawyer, Med Spa, Accountant, Restaurant.",
      "editor": "textfield",
      "prefill": "Dentist"
    },
    "maxBusinesses": {
      "title": "Max businesses to consider",
      "type": "integer",
      "description": "How many Maps listings LeadGap will inspect before filtering.",
      "default": 40,
      "minimum": 1,
      "maximum": 500,
      "editor": "number"
    },
    "maxResults": {
      "title": "Max qualified leads to return",
      "type": "integer",
      "description": "Upper limit of qualified opportunities written to the dataset.",
      "default": 20,
      "minimum": 1,
      "maximum": 200,
      "editor": "number"
    },
    "minOpportunityScore": {
      "title": "Minimum opportunity score",
      "type": "integer",
      "description": "Raise this to receive fewer but stronger leads.",
      "default": 0,
      "minimum": 0,
      "maximum": 100,
      "editor": "number"
    },
    "websites": {
      "title": "Manual website audit (optional)",
      "type": "array",
      "description": "Skip discovery and audit these URLs directly.",
      "editor": "stringList",
      "items": {"type": "string", "pattern": "^https?://.+"},
      "default": []
    }
  },
  "required": ["country"]
}
